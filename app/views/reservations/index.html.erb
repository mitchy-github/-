<div class="container-fiuld room-posts">
  <h4 class="pb-4">予約済みルーム一覧</h4>
  <table>
    <thead>
      <tr align="center">
        <th>id</th>
        <th>ルーム画像</th>
        <th>ルーム名</th>
        <th>ルーム紹介</th>
        <th>合計料金</th>
        <th>開始日</th>
        <th>終了日</th>
        <th>予約確定日時</th>
      </tr>
    </thead>
    <tbody>
      <% if @reservations.count > 0 %>
        <% @reservations.each do |reservation| %>
          <tr>
            <td class="posts-id">
              <%= reservation.id %>
            </td>
            <td>
              <% if reservation.room.room_image.attached? %>
                <%= image_tag reservation.room.room_image, size: '100' %>
              <% else %>
                画像なし
              <% end %>
            </td>
            <td class="posts-name"><%= reservation.room.room_name %></td>
            <td class="posts-content"><%= reservation.room.room_content %></td>
            <td class="price-icon"><%= reservation.reservation_price %></td>
            <td><%= reservation.reservation2_start %></td>
            <td><%= reservation.reservation2_end %></td>
            <td><%= reservation.created_at.to_s(:datetime_jp) %></td>
            <% end %>
          <% else %>
        <td colspan="8">現在予約済みのお部屋は有りません</td>
      <% end %>
    </tbody>
  </table>
</div>